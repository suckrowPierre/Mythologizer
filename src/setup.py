from setuptools import setup, find_packages

setup(
    name="mythologizer",
    version="0.0.1",
    packages=find_packages(),
    install_requires=[],
    description="A simulation package for myth agents and attributes.",
)